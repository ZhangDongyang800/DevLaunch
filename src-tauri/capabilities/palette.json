{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "palette",
  "description": "Capability for the search palette window",
  "windows": ["palette"],
  "permissions": ["core:default"]
}
